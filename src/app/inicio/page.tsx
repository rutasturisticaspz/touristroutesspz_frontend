// El sitio anterior respondía la portada tanto en / como en /inicio, y
// el enlace del menú apunta a /inicio. Se mantiene para no romper
// enlaces ya publicados.
export { default } from '../page';
